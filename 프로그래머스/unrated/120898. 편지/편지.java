class Solution {
    public int solution(String message) {
        int answer = 0;
        String[] div = message.split(""); // split을 안해도 공백까지 length에 포함됨.....
       
        return div.length*2;
    }
}
